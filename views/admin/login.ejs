<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Connexion admin  Club scolaire</title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <header class="site-header">
    <div class="container header-content">
      <a href="/" class="logo">Club scolaire</a>
      <nav class="main-nav">
        <a href="/" class="nav-link">Accueil</a>
      </nav>
    </div>
  </header>

  <main class="section">
    <div class="container">
      <% if (flash) { %>
        <div class="flash"><%= flash %></div>
      <% } %>

      <section class="card" style="max-width:420px;margin:2rem auto;">
        <h1>Espace administrateur</h1>
        <form action="/admin/login" method="post" class="form">
          <div class="form-row">
            <label for="username">Identifiant</label>
            <input type="text" id="username" name="username" />
          </div>
          <div class="form-row">
            <label for="password">Mot de passe</label>
            <input type="password" id="password" name="password" />
          </div>
          <% if (error) { %>
            <p class="error-inline"><%= error %></p>
          <% } %>
          <div class="form-row">
            <button type="submit" class="button button--primary">Se connecter</button>
          </div>
        </form>
        <form action="/admin/setup" method="get" class="form" style="margin-top:0.75rem;">
          <div class="form-row">
            <button type="submit" class="button" style="width:100%;text-align:center;background:#16a34a;border-color:#16a34a;">Créer un compte administrateur</button>
          </div>
        </form>
      </section>
    </div>
  </main>

  <footer class="site-footer">
    <div class="container">
      <p class="muted"> <%= new Date().getFullYear() %> Club scolaire</p>
    </div>
  </footer>
</body>
</html>
