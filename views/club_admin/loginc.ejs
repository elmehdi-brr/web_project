<!DOCTYPE html>
<html lang="fr">
<head>
  <meta charset="UTF-8" />
  <title>Connexion admin  <%= club.name %></title>
  <meta name="viewport" content="width=device-width, initial-scale=1" />
  <link rel="stylesheet" href="/css/styles.css" />
</head>
<body>
  <header class="site-header">
    <div class="container header-content">
      <a href="/" class="logo">Club scolaire</a>
      <nav class="main-nav">
        <a href="/" class="nav-link">Accueil</a>
      </nav>
    </div>
  </header>

  <main class="section">
    <div class="container">
      <% if (flash) { %>
        <div class="flash"><%= flash %></div>
      <% } %>

      <section class="card" style="max-width:420px;margin:2rem auto;">
        <h1>Espace admin du club <%= club.name %></h1>
        <form action="/clubs/<%= club.id %>/admin/login" method="post" class="form">
          <div class="form-row">
            <label for="username">Identifiant</label>
            <input type="text" id="username" name="username" />
          </div>
          <div class="form-row">
            <label for="password">Mot de passe</label>
            <input type="password" id="password" name="password" />
          </div>
          <% if (error) { %>
            <p class="error-inline"><%= error %></p>
          <% } %>
          <div class="form-row">
            <button type="submit" class="button button--primary">Se connecter</button>
          </div>
        </form>
      </section>
    </div>
  </main>

  <footer class="site-footer">
    <div class="container">
      <p class="muted"> <%= new Date().getFullYear() %> Club scolaire</p>
    </div>
  </footer>
</body>
</html>
